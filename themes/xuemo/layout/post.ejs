<div id="article-view" class="fade-in">
  <button onclick="location.href='/'" class="mb-6 flex items-center text-blue-600 hover:text-blue-800 dark:text-blue-400 dark:hover:text-blue-300 transition font-medium">
    <i class="fa-solid fa-arrow-left mr-2"></i> 返回首页
  </button>
  
  <article class="bg-white dark:bg-gray-800 rounded-2xl shadow-sm p-8 md:p-12 border border-gray-100 dark:border-gray-700 transition-colors duration-300">
    <!-- 👇 新增判断：只有当文章没有设置 hide_title: true 时，才显示标题区域 -->
    <% if (!page.hide_title) { %>
    <div class="mb-8 border-b border-gray-100 dark:border-gray-700 pb-8">
      <div class="flex items-center text-sm text-gray-500 dark:text-gray-400 mb-3 space-x-4">
        <span><i class="far fa-calendar-alt mr-1"></i> <%= date(page.date, 'YYYY-MM-DD') %></span>
        <span class="px-2 py-0.5 bg-gray-100 dark:bg-gray-700 rounded text-xs text-gray-600 dark:text-gray-300">
          <%= (page.categories && page.categories.length > 0) ? page.categories.data[0].name : '技术' %>
        </span>
      </div>
      <h1 class="text-3xl md:text-4xl font-extrabold text-gray-900 dark:text-white leading-tight">
        <%= page.title %>
      </h1>
    </div>
    <% } %>
    
    <!-- 正文内容 (Hexo 会自动渲染 Markdown) -->
    <div class="markdown-body text-lg">
      <%- page.content %>
    </div>
  </article>
</div>